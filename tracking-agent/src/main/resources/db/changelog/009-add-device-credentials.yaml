databaseChangeLog:

  - changeSet:
      id: 009-add-device-credentials
      author: rupesh
      changes:

        - addColumn:
            tableName: agent_device_state
            columns:

              - column:
                  name: device_id
                  type: VARCHAR(64)

              - column:
                  name: device_secret
                  type: VARCHAR(255)
